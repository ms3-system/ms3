# metadata-service local configuration
# Copy to .env.example and source it, or export these directly. All are optional —
# the service runs with these defaults if nothing is set.

# Port the HTTP server listens on.
METADATA_SERVICE_SERVER_PORT=8083

# Path to the bbolt database file. Created on demand, including parent dirs.
METADATA_SERVICE_DB_PATH=data/metadata.db
